int mode;    // LED 밝기 조절을 하는 변수입니다.

void setup() {
  pinMode(5, OUTPUT);   //
  pinMode(2, INPUT);
  mode = 0;    
}

void loop() {
  if (digitalRead(2)==HIGH) {
       mode=(mode+1)%2;  
       if (mode==1) digitalWrite(5, HIGH); 
       if (mode==0) digitalWrite(5, LOW);  
  }
}
